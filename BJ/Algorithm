--this is the Algorithm File--
pip install mysql-connector-python

# connects user to MySQL database
connection = mysql.connector.connect(
    host="localhost",
    user="root",
    database="menu_recommender"
)
# Create a cursor object (can run SQL commands)
cursor = connection.cursor()

def CollectUserData():
    while True:
        item = input("Please input the ingredient you would like to use: ")
        amount = int(input("How many do you have? "))
        if item.lower() == "exit":
            break




cursor.close()
connection.close()